# Kopie als .env ablegen: cp .env.example .env
# VITE_API_URL leer lassen → der API-Host wird automatisch vom App-Host abgeleitet
# (localhost lokal, LAN-IP beim Netzwerk-Test vom Handy). Nur für Native-Builds
# oder einen festen Host explizit setzen:
# VITE_API_URL=http://192.168.1.50:3010/api/v1
